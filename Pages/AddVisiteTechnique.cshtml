@page
@model RPtest.Pages.AddVisiteTechniqueModel
@{
    Layout = "Dashboard";
    ViewData["Title"] = Model.IsNew ? "Ajouter une Visite Technique" : "Modifier une Visite Technique";
    decimal Total = 0;
}

<h1>@ViewData["Title"]</h1>
<h4>Véhicule: @Model.VehiculeMarque @Model.VehiculeModel</h4>
<hr />

<div class="row">
    <div class="col-md-4">
        <form method="post">
            <input type="hidden" asp-for="VisiteTechnique.Id" />
            <input type="hidden" asp-for="VisiteTechnique.VehiculeId" />
            <input type="hidden" asp-for="IsNew" />

            <div class="form-group">
                <label asp-for="VisiteTechnique.Montant" class="control-label"></label>
                <input asp-for="VisiteTechnique.Montant" type="number" class="form-control" />
                <span asp-validation-for="VisiteTechnique.Montant" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="VisiteTechnique.Date" class="control-label"></label>
                <input asp-for="VisiteTechnique.Date" class="form-control" type="date" />
                <span asp-validation-for="VisiteTechnique.Date" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="VisiteTechnique.Description" class="control-label"></label>
                <textarea asp-for="VisiteTechnique.Description" class="form-control"></textarea>
                <span asp-validation-for="VisiteTechnique.Description" class="text-danger"></span>
            </div>

            <div class="form-group">
                @if (Model.IsNew)
                {
                    <button type="submit" class="btn btn-primary">Ajouter</button>
                }
                else
                {
                    <button type="submit" class="btn btn-primary">Enregistrer</button>
                }
                <a asp-page="ListVidange" asp-route-vehiculeId="@Model.VehiculeId" class="btn btn-secondary">Annuler</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}